COPY dwh.lk_anagrafica_indicatori (id, nome, dim_sigla, query, ruolo_id, dim_descrizione) FROM stdin;
18	Numero di concorsi banditi con accertamento di competenze trasversali	D3	select ROUND(sum(valore_num)::numeric,2) from ft_questionari_globale where raw_element like '4.2' and rilevazione like '#rilevazione#' and cf_ente like '#codicefiscaleente#'	2	RECRUITING
20	Numero di percorsi formativi per accrescere le competenze trasversali 	D4	select ROUND(sum(valore_num)::numeric,2) from ft_questionari_globale where raw_element like '4.3' and rilevazione like '#rilevazione#'  and cf_ente like '#codicefiscaleente#'	2	SVILUPPO PROFESSIONALE
8	Totale percorsi formativi attivati negli Enti monitorati	D4	select ROUND(sum(valore_num)::numeric,2) from ft_questionari_globale where raw_element like '4.2' and rilevazione like '#rilevazione#'  and progetto like '%#progetto#%'	1	SVILUPPO PROFESSIONALE
21	Attivazione di modelli di incentivazione e riconoscimento non monetari	D5	select valore from ft_questionari_globale where raw_element like '5.2' and rilevazione like '#rilevazione#'  and cf_ente like '#codicefiscaleente#'	2	REWARDING E SVILUPPO DI CARRIERA
10	Totale di convenzioni attivate dagli Enti con Università e Centri di alta formazione per lo sviluppo delle risorse umane	D5	select ROUND(sum(valore_num)::numeric,2) from ft_questionari_globale where raw_element like '5.3' and rilevazione like '#rilevazione#'  and progetto like '%#progetto#%'	1	REWARDING E SVILUPPO DI CARRIERA
7	Totale percorsi formativi volti ad accrescere le competenze trasversali delle Risorse Umane	D4	select ROUND(sum(valore_num)::numeric,2) from ft_questionari_globale where raw_element like '4.3' and rilevazione like '#rilevazione#' and progetto like '%#progetto#%'	1	SVILUPPO PROFESSIONALE
12	Totale posizioni non dirigenziali negli enti monitorati	D6	select ROUND(sum(valore_num)::numeric,2) from ft_questionari_globale where raw_element like '6.3.b' and rilevazione like '#rilevazione#'  and progetto like '%#progetto#%'	1	CAPACITY BUILDING E PERFORMANCE ORGANIZZATIVA
16	Numero di unità di personale assunto a tempo indeterminato under 35	D2	select ROUND(sum(valore_num)::numeric,2) from ft_questionari_globale where raw_element like '2.2' and rilevazione like '#rilevazione#'  and cf_ente like '#codicefiscaleente#'	2	PROGRAMMAZIONE DEL FABBISOGNO DI PERSONALE
1	Numero Amministrazioni monitorate	D1	select count(distinct cf_ente) from dwh.ft_questionari_globale where progetto like '%#progetto#%'	1	RILEVAZIONE E CLASSIFICAZIONE DI PROFESSIONI E COMPETENZE
13	Profili professionali attivi nell'ente	D1	select ROUND(sum(valore_num)::numeric,2) from ft_questionari_globale where raw_element like '1.5' and rilevazione like '#rilevazione#'  and cf_ente like '#codicefiscaleente#'	2	RILEVAZIONE E CLASSIFICAZIONE DI PROFESSIONI E COMPETENZE
15	Numero di unità di personale assunto a tempo indeterminato	D2	select ROUND(sum(valore_num)::numeric,2) from ft_questionari_globale where raw_element like '2.1' and rilevazione like '#rilevazione#' and cf_ente like '#codicefiscaleente#'	2	PROGRAMMAZIONE DEL FABBISOGNO DI PERSONALE
2	Percentuale Adozione del modello professionale basato sulle competenze	D1	select ROUND(SUM(case when elementid = '1.1' then valore_num else 0 end)/(count(distinct(ente)))::numeric,2) as valore from dwh.ft_questionari_globale where rilevazione = '#rilevazione#'  and progetto like '%#progetto#%' and ft_questionari_globale.faseelaborazione = 'Chiusa'	1	RILEVAZIONE E CLASSIFICAZIONE DI PROFESSIONI E COMPETENZE
17	Numero di concorsi banditi	D3	select ROUND(sum(valore_num)::numeric,2) from ft_questionari_globale where raw_element like '3.1' and rilevazione like '#rilevazione#'  and cf_ente like '#codicefiscaleente#'	2	RECRUITING
4	Totale nuovi assunti negli enti monitorati a tempo indeterminato under 35 	D2	select ROUND(sum(valore_num)::numeric,2) from ft_questionari_globale where raw_element like '2.2' and rilevazione like '#rilevazione#'  and progetto like '%#progetto#%'	1	PROGRAMMAZIONE DEL FABBISOGNO DI PERSONALE
3	Totale nuovi assunti negli enti monitorati a tempo indeterminato	D2	select ROUND(sum(valore_num)::numeric,2) from ft_questionari_globale where raw_element like '6.12.a' and rilevazione like '#rilevazione#'  and progetto like '%#progetto#%'	1	PROGRAMMAZIONE DEL FABBISOGNO DI PERSONALE
9	Percentuale modelli di incentivazione e riconoscimento non monetari adottati tra gli Enti monitorati	D5	select ROUND((SUM(case when elementid = '5.2' then valore_num else 0 end)/(count(distinct(ente)))*100) :: numeric,2) as valore from dwh.ft_questionari_globale where rilevazione = '#rilevazione#' and progetto like '%#progetto#%' and ft_questionari_globale.faseelaborazione = 'Chiusa'	1	REWARDING E SVILUPPO DI CARRIERA
6	Totale concorsi banditi dagli enti monitorati	D3	select ROUND(sum(valore_num)::numeric,2) from ft_questionari_globale where raw_element like '3.2' and rilevazione like '#rilevazione#'  and progetto like '%#progetto#%'	1	RECRUITING
19	Numero di percorsi formativi rivolti al proprio personale	D4	select ROUND(sum(valore_num)::numeric,2) from ft_questionari_globale where raw_element like '4.2' and rilevazione like '#rilevazione#'  and cf_ente like '#codicefiscaleente#'	2	SVILUPPO PROFESSIONALE
22	Numero di convenzioni attive per alta formazione del personale	D5	select ROUND(sum(valore_num)::numeric,2) from ft_questionari_globale where raw_element like '5.3' and rilevazione like '#rilevazione#'  and cf_ente like '#codicefiscaleente#'	2	REWARDING E SVILUPPO DI CARRIERA
23	Numero totale dei processi formalmente mappati dall'amministrazione 	D6	select ROUND(sum(valore_num)::numeric,2) from ft_questionari_globale where raw_element like '6.1' and rilevazione like '#rilevazione#'  and cf_ente like '#codicefiscaleente#'	2	CAPACITY BUILDING E PERFORMANCE ORGANIZZATIVA
14	Adozione di una library dei processi di lavoro (Si/NO)	D1	select valore from ft_questionari_globale where raw_element like '1.3' and rilevazione like '#rilevazione#'  and cf_ente like '#codicefiscaleente#'	2	RILEVAZIONE E CLASSIFICAZIONE DI PROFESSIONI E COMPETENZE
11	Totale posizioni dirigenziali negli enti monitorati	D6	select ROUND(sum(valore_num)::numeric,2) from ft_questionari_globale where raw_element like '6.3.a' and rilevazione like '#rilevazione#' and progetto like '%#progetto#%'	1	CAPACITY BUILDING E PERFORMANCE ORGANIZZATIVA
25	Percentuale assunzioni determinate dal turnover per ciascuna amministrazione	D2	select (SUM(case when elementid = '2.6' then valore_num else 0 end) / SUM(case when elementid = '2.3' then valore_num else 0 end)) from dwh.ft_questionari_globale where rilevazione = '#rilevazione#' and ft_questionari_globale.faseelaborazione = 'Chiusa' and cf_ente like '#codicefiscaleente#' group by ente	2	PROGRAMMAZIONE DEL FABBISOGNO DI PERSONALE
26	Percentuale di amministrazioni che prevedono lintegrazione tra il sistema di valutazione della performance ed il Sistema Professionale competency based	D5	select ROUND((SUM(case when elementid = '5.1' then valore_num else 0 end) / count(distinct(ente)))::numeric,2) from dwh.ft_questionari_globale where rilevazione = '#rilevazione#' and ft_questionari_globale.faseelaborazione = 'Chiusa' 	1	REWARDING E SVILUPPO DI CARRIERA
27	Personale in servizio (Conti Annuali 2023)	D2	select SUM(o."PERSONALE_TEMPO_PIENO_UOMINI") + SUM(o."PERSONALE_TEMPO_PIENO_DONNE") from ca.ft_occupazione o inner join ca.lk_istituzioni i on o."ISTITUZIONE" = i.istituzione_id where o.anno = 2023 and  i."CODI_FISCALE" = '#codicefiscaleente#'	2	PROGRAMMAZIONE DEL FABBISOGNO DI PERSONALE
\.
